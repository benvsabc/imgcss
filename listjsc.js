(function(){function swipeBookDetail($target){var name=$target.attr('data-name');var author=$target.attr('data-author');var intr=$target.attr('data-intr');var url=$target.attr('data-url');var $book=$target.closest('.swiper-wrapper').find('.swiper-book');var $name=$book.find('.name').text(name);if($name.is('a')){$name.attr('href',url);}
$book.find('.book-link').attr('href',url);$book.find('.a-link').attr('href',url);var $author=$book.find('.author').text(author);if(!$author.is('a')){$author.attr('href','/search?author='+author);}
$book.find('.intr').text(intr);$book.find('img').attr('src',$target.find('img').attr('src'));var score=$target.attr('data-score');$book.find('.score-num').text(score);var typeName=$target.attr('data-type-name');var type=$target.attr('data-type');$book.find('.type').attr('href','/search/stack?type='+type).text(typeName);}
(function(){var $swiper=$('.small-swiper .swiper-container').swiper().on({swipestart:function(){swipeBookDetail($swiper.offsetTarget(0));},click:function(event){$swiper.swipeTo($(event.target).closest('.swiper-item').index());}});}());}())